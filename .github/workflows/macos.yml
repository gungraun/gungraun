name: MacOS checks

on:
  workflow_call:
    inputs:
      cargo_term_color:
        required: false
        type: string

env:
  CARGO_TERM_COLOR: ${{ inputs.cargo_term_color }}

jobs:
  build_hack:
    name: Build with feature powerset
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-bin: false
      - uses: taiki-e/install-action@just
      - uses: taiki-e/install-action@cargo-hack
      - name: Build and install valgrind
        run: |
          brew tap LouisBrunner/valgrind
          brew install --HEAD LouisBrunner/valgrind/valgrind
          echo "VALGRIND_REQUESTS_VALGRIND_INCLUDE=$(brew --prefix)/include" >> "$GITHUB_ENV"
      - name: Update dependencies
        run: cargo update
      - name: Build
        run: just build-hack

  test:
    name: Run unit and integrations tests
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - name: "Prepare: Run cargo update"
        run: cargo update
      - uses: Swatinem/rust-cache@v2
        with:
          cache-bin: false
      - uses: taiki-e/install-action@cargo-hack
      - uses: taiki-e/install-action@just
      - name: Build and install valgrind
        run: |
          brew tap LouisBrunner/valgrind
          brew install --HEAD LouisBrunner/valgrind/valgrind
          echo "VALGRIND_REQUESTS_VALGRIND_INCLUDE=$(brew --prefix)/include" >> "$GITHUB_ENV"
      - name: Build all tests with the feature powerset
        run: just build-tests-hack
      - name: Run the tests
        run: cargo test --workspace --exclude valgrind-requests-tests
